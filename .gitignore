node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Tauri sidecar binaries (build artifacts — never commit)
src-tauri/binaries/
*.bin
*.deb
*.rpm
*.AppImage

# ============================================================================
# SOVEREIGN SECURITY (from sovereign-pair .gitignore)
# ============================================================================
# CRÍTICO: Estes arquivos NUNCA devem ser commitados

# Gitleaks (licença e allowlist local — nunca commitar)
.gitleaks.key
.gitleaks.toml

# SAST/SCA scanner configs (local only)
.trivyignore
.semgrepignore

# Terraform vars (chaves, senhas, OCI auth)
*.tfvars
*.tfvars.json
*.tfstate
*.tfstate.backup

# Runtime databases
*.sqlite
*.jsonl
